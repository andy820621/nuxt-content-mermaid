name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  source-verification:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - run: npm i -g --force corepack@latest && corepack enable

      - name: Install dependencies
        run: npx nypm@latest i

      - name: Playground prepare
        run: npm run dev:prepare

      - name: Install Playwright browsers
        run: npx playwright install

      - name: Verify source
        run: pnpm verify:source

  representative-compatibility-matrix:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - profile: nuxt-3-minimum
            node-version: '22.21.1'
          - profile: nuxt-4-minimum
            node-version: '22.21.1'
          - profile: nuxt-3-known-latest
            node-version: '22.21.1'
          - profile: nuxt-4-known-latest
            node-version: '22.21.1'
          - profile: nuxt-3-minimum-content-known-latest
            node-version: '22.21.1'
          - profile: nuxt-4-known-latest-content-minimum
            node-version: '22.21.1'

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - run: npm i -g --force corepack@latest && corepack enable

      - name: Install dependencies
        run: npx nypm@latest i

      - name: Prepare Nuxt types and module stubs
        run: npm run dev:prepare

      - name: Install Playwright browsers
        run: npx playwright install

      - name: Verify Compatibility Profile
        run: npm run test:compatibility-profile -- --profile ${{ matrix.profile }}

  module-compatibility:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        nuxt-version: ['3.20.1', '4.1.0']

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - run: npm i -g --force corepack@latest && corepack enable

      - name: Select the supported Nuxt line
        run: >-
          pnpm pkg set
          devDependencies.nuxt=${{ matrix.nuxt-version }}
          devDependencies.@nuxt/schema=${{ matrix.nuxt-version }}
          dependencies.@nuxt/kit=${{ matrix.nuxt-version }}

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Prepare Nuxt types and module stubs
        run: pnpm dev:prepare

      - name: Verify module configuration fixtures
        run: pnpm vitest run test/moduleConfiguration.e2e.test.ts test/moduleConfigurationMigration.e2e.test.ts
